/* This is logic used to modify variables - ie. if we need to format product key or display different texts */
export const logic =  "versionNew = version == 'v1' ? version + ' new modified for 1' : version + ' new modified for 2'"